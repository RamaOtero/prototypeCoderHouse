import CartWidget from "../cartWidget/cartWidget"

const navBar = () => {
    return (
    <nav>
      <div>
         <h1>Prototype</h1>
      </div>
        <CartWidget />
    </nav>

    )
}

    export default navBar